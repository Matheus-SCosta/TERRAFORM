version: 3
projects:
- dir: .
  workflow: AWS/configurando_role_terraform
workflows:
  custom1:
    plan:
      steps:
      - init
      - run: echo "INICIANDO PLAN PLAN PLAN PLAN"
      - plan
    apply:
      steps:
      - run: echo "INICIANDO APPLY APPLY APPLY APPLY"
      - run: my custom apply command