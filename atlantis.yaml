version: 3
projects:
- dir: AWS/configurando_role_terraform
  workflow: custom1
workflows:
  custom1:
    plan:
      steps:
      - init
      - run: echo "Executando terraform plan"
      - plan
    apply:
      steps:
      - init
      - run: echo "Executando terraform apply"
      - apply
    